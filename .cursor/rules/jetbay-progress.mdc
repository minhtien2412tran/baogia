---
description: JETBAY progress board and monorepo boundaries (always on)
alwaysApply: true
---

# JETBAY agent context

1. Start from `docs/CONTINUE_AT_HOME.md` + `docs/JETBAY_PRODUCT_MAP.md` — do not invent status from chat.
2. **Product = jetbay.com clone in `apps/web`**. `m-tien.com/jet-bay` is sales/quote only, not the product UI.
3. After meaningful work, update `docs/CONTINUE_AT_HOME.md` and the checklist log.
4. Scope by app: `apps/web` = FE clone, `apps/api` = BE, `apps/admin` = dashboard. Branches `feat/web-*`, `feat/api-*`, `feat/admin-*`.
5. Features only from `docs/JETBAY_BAO_GIA.md` + `docs/JETBAY_DANH_GIA_KY_THUAT.md`; security may mirror HomeFix but never share secrets/DB.
6. Never commit `.env` / `.env.local` or print production secrets. UI package: `@jetbay/ui`.
