# JETBAY local assets

Mirror of `https://asserts.jet-bay.com` for offline / clean-room clone.

Re-download:

```bash
pnpm assets:jetbay
```

Runtime paths are served from `/assets/jetbay/...` via `apps/web/src/config/jetbay-cdn.ts`.
